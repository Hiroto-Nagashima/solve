<%= form_with model:comment, url: post_comments_path(post) do |f| %>
    <%= f.text_area :content, autofocus: true,class: "input-area" %>
    <%= f.submit "送信", class: "btn btn-outline-dark comment-submit float-right" %>
<% end %>